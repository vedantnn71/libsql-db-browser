export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "SQLite DB Browser",
  description:
    "SQLite DB Browser is a simple and lightweight SQLite database browser",
  mainNav: [
  ],
}
